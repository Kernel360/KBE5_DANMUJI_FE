import LoginPage from './pages/LoginPage';

function App() {
  return (
    <div style={{
      minHeight: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: '#f9fafb'
    }}>
      <LoginPage />
    </div>
  );
}

export default App;