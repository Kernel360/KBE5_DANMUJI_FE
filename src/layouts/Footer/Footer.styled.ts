import styled from "styled-components";

export const Footer = styled.footer`
  position: absolute;
  bottom: 24px;
  left: 50%;
  transform: translateX(-50%);
  color: #9ca3af;
  font-size: 0.75rem;
  text-align: center;
`;

